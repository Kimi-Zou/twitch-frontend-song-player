import { makeStyles } from '@material-ui/core/styles';

const useVolumeControlsStyles = makeStyles({
  root: {
    flexGrow: 1,
  },
  container: {
    flexWrap: 'nowrap',
  },
});

export default useVolumeControlsStyles;
