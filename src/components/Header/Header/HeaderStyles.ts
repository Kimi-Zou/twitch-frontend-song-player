import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  appBarStyles: {
    position: 'static',
    padding: '12px 12px',
    marginBottom: 30,
  },
}));

export default useStyles;
