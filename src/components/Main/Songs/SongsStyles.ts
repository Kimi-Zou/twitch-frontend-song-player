import { Theme, createStyles, makeStyles } from '@material-ui/core/styles';

const useSongsStyles = makeStyles((theme: Theme) =>
  createStyles({
    container: {
      marginBottom: 110,
    },
  }),
);

export default useSongsStyles;
